---
생성날짜: 2026-04-17 12:00
마지막수정날짜: 2026-04-17-목요일 12:00
tags:
- 핵심기술
- 포트폴리오
type:
- 기술문서
---

# THE FORGE 핵심 기술 문서

---

## 0. 이 문서의 대상과 읽는 순서

**대상 독자:** THE FORGE를 처음 접하는 개발자, 포트폴리오 리뷰어

**핵심 한 줄 요약:** `forge run "요청"` 한 번이면, 기획부터 구현, 평가까지 자동으로 돌아간다 -- API 비용 0원으로.

**문서 구성:**
- 각 섹션은 **Why(기존 한계)** → **How(구현 방식)** → **What(핵심 특성)** 구조를 따른다
- 섹션 순서는 실제 `forge run` 실행 흐름과 일치한다
- 섹션 9는 다른 프로젝트에서 THE FORGE를 도구로 사용할 때 필요한 실전 레퍼런스다

| 섹션 | 다루는 내용 | 핵심 질문 |
|------|-----------|----------|
| 1 | 전체 아키텍처 | 이 시스템은 뭐고 왜 만들었나? |
| 2 | 자동 스프린트 루프 | 어떻게 자동으로 돌아가나? |
| 3 | 3-에이전트 파이프라인 | 각 에이전트가 뭘 받고 뭘 내보내나? |
| 4 | artifacts/ 파일 통신 | 에이전트끼리 어떻게 소통하나? |
| 5 | 체크포인트 복구 | 중간에 죽으면 어떻게 되나? |
| 6 | Telegram 양방향 통신 | 밖에서 어떻게 제어하나? |
| 7 | 비용 투명성 | 토큰을 얼마나 쓰고 있는지 어떻게 아나? |
| 8 | 프로젝트 부트스트래핑 | 새 프로젝트에 어떻게 적용하나? |
| 9 | 실전 사용 가이드 | 명령어 뭐 치면 되나? |

---

## 1. 전체 아키텍처: Harness-over-Harness

### 선택 이유

| 기존 방식의 한계 | THE FORGE의 해법 |
|-----------------|-----------------|
| Claude API를 직접 호출하면 토큰당 과금 발생 | Claude Code CLI(Max 정액제)를 subprocess로 호출 -- **API 비용 0원** |
| 단일 세션에서 기획+구현+평가 → 자기 합리화(Self-Rationalization) 발생 | 3개의 독립 프로세스로 역할 격리 |
| bash 스크립트 기반 하네스는 Windows 미지원, 에러 처리 취약 | Python 패키지로 전환 -- 크로스 플랫폼, Pydantic 모델 기반 |
| 프로젝트마다 하네스 코드를 복사해야 함 | `uv tool install`로 1회 설치, N개 프로젝트에서 재사용 |

### 구현 방법

- **구현 파일:** `src/forge/orchestrator.py`, `src/forge/cli.py`
- **핵심 로직:**
  - `forge` CLI가 `subprocess.run(["claude", ...])` 으로 Claude Code CLI를 호출
  - Planner/Evaluator: `claude -p --agent {name}` (비대화형 subprocess)
  - Generator: `claude` (대화형, stdin/stdout 직접 연결)
  - Max 플랜의 쿼터 안에서 동작하므로 토큰 사용량과 무관하게 월정액
- **참고 문서:** [Claude Code CLI](https://docs.anthropic.com/en/docs/claude-code), [typer](https://typer.tiangolo.com/), [Pydantic Settings](https://docs.pydantic.dev/latest/concepts/pydantic_settings/)

### 핵심 아키텍처 다이어그램

```
┌─ THE_FORGE (uv tool install로 1회 설치) ─────────────────────┐
│                                                               │
│  forge run ──> Orchestrator (자동 스프린트 루프)                │
│                                                               │
│  ┌─ Sprint Loop ──────────────────────────────────────┐      │
│  │                                                     │      │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────┐     │      │
│  │  │ Planner  │───>│Generator │───>│Evaluator │     │      │
│  │  │subprocess│    │interactive│    │subprocess│     │      │
│  │  │claude -p │    │ claude   │    │claude -p │     │      │
│  │  └────┬─────┘    └────┬─────┘    └────┬─────┘     │      │
│  │       │               │               │            │      │
│  │       v               v               v            │      │
│  │  spec.md         src/, tests/     qa-report.md     │      │
│  │  sprint-contract  progress-log     PASS / FAIL     │      │
│  │                                                     │      │
│  │  PASS ──> Telegram 승인 ──> 다음 스프린트            │      │
│  │  FAIL ──> Telegram 선택지 ──> 재진입 or 중단         │      │
│  └─────────────────────────────────────────────────────┘      │
│                                                               │
│  ┌─ 부가 시스템 ──────────────────────────────────────┐      │
│  │ SprintTracer: 시간 + 토큰 추적 (Langfuse 선택적)    │      │
│  │ Telegram: 알림 + 원격 명령 수신                      │      │
│  │ Checkpoint: 크래시 자동 복구                          │      │
│  └─────────────────────────────────────────────────────┘      │
└───────────────────────────────────────────────────────────────┘
         │
         v (프로젝트 디렉토리에서 실행)
┌─ 대상 프로젝트 (/path/to/my-project) ──────────────────────┐
│  src/  tests/  CLAUDE.md  .env  pyproject.toml              │
│  artifacts/ (에이전트 간 통신 -- forge가 관리)               │
└─────────────────────────────────────────────────────────────┘
```

### 핵심 특성

- THE FORGE는 **도구**다. 프로젝트 코드에 포함되지 않는다. ruff, pytest처럼 별도 설치하여 사용
- Windows / macOS / Linux 크로스 플랫폼 지원
- API 키 불필요 -- Max 플랜 구독만 있으면 동작
- `uv tool install .` 한 줄로 설치, 어디서든 `forge` 명령 사용 가능

---

## 2. 자동 스프린트 루프 (Automatic Sprint Loop)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 1 스프린트 실행 후 수동으로 `forge run` 재실행 필요 | 야간/장시간 작업 시 사람이 계속 붙어있어야 함 |
| FAIL 시 수동으로 재진입 명령 실행 | 자동 대처 + 원격 선택지 필요 |
| 무한 루프 시 토큰/시간 낭비 방지 수단 없음 | 안전장치(Safety Guard) 필수 |

### 구현 방법

- **구현 파일:** `src/forge/orchestrator.py` -- `run_cycle()` 함수
- **핵심 로직:**
  - `run_cycle()` 내부의 `while True` 루프가 스프린트를 반복 실행
  - 루프 1회 = Contract 생성 → Generator 실행 → Evaluator 평가 → 결과 판정
  - PASS 시: sprint-N-done.md로 아카이브 → `has_next_sprint` frontmatter 확인 → 다음 스프린트
  - FAIL 시: 체크포인트를 `CONTRACT_DONE`으로 자동 롤백 → 사용자에게 3가지 선택지 제공
- **참고 문서:** [Anthropic Engineering Blog](https://www.anthropic.com/engineering)

### 스프린트 루프 흐름도

```
forge run "요청"
    │
    v
[Phase 1: Planning] ──── 프로젝트당 1회만 실행
    │ Planner가 spec.md + specs/*.md 생성
    │ Telegram으로 승인 요청
    v
┌─────────────────── Sprint Loop (while True) ───────────────┐
│                                                             │
│  ┌─ 안전장치 체크 ────────────────────────────────────┐    │
│  │ - 연속 FAIL 3회 도달?                               │    │
│  │ - 누적 시간 > max_total_minutes (1440분)?           │    │
│  │ - 스프린트 수 > max_total_sprints (20)?             │    │
│  │ - /stop 시그널 수신?                                │    │
│  │   -> 하나라도 해당 시 자동 중단 + Telegram 알림      │    │
│  └─────────────────────────────────────────────────────┘    │
│       │                                                     │
│       v                                                     │
│  [Phase 2: Contract]                                        │
│       │ Planner(Mode C)가 sprint-contract.md 생성           │
│       │ YAML frontmatter: has_next_sprint, next_preview     │
│       v                                                     │
│  [Phase 3: Generator]                                       │
│       │ interactive 세션 -- 코드 구현                        │
│       │ progress-log.md 업데이트 후 세션 종료                │
│       v                                                     │
│  [Phase 4: Evaluator]                                       │
│       │ qa-report.md 생성 (PASS/FAIL + 4차원 점수)          │
│       v                                                     │
│  [Phase 5: 결과 판정]                                       │
│       │                                                     │
│       ├── PASS + has_next_sprint: true                      │
│       │    -> Telegram: 점수 + 다음 스프린트 미리보기        │
│       │    -> /resume 대기 -> 루프 계속                     │
│       │                                                     │
│       ├── PASS + has_next_sprint: false                     │
│       │    -> Telegram: 프로젝트 완료 알림                  │
│       │    -> 루프 종료                                     │
│       │                                                     │
│       └── FAIL                                              │
│            -> 체크포인트 CONTRACT_DONE으로 롤백              │
│            -> Telegram: 실패 요약 + 선택지                  │
│            -> /resume: Generator 재진입                     │
│            -> /eval: Evaluator만 재실행                     │
│            -> /stop: 루프 종료                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 핵심 특성

- `forge run` 한 번 실행으로 프로젝트 완료까지 자동 진행 (v2.3 기본 동작)
- 스프린트 경계에서만 Telegram 승인 대기 -- 외출 중에도 폰으로 제어
- `--single-sprint`: v2.2 호환 모드 (1 스프린트만 실행)
- `--max-sprints N`: 최대 N개 스프린트만 실행하는 안전장치
- `has_next_sprint` frontmatter로 프로젝트 완료를 자동 판단 (Planner가 마지막 스프린트에 `false` 설정)

---

## 3. 3-에이전트 파이프라인 (3-Agent Pipeline)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 단일 세션에서 기획+구현+평가 수행 시 **자기 합리화** 발생 | 프로세스 격리로 역할별 독립 판단 보장 |
| "이 정도면 괜찮겠지"가 구현 중에 떠오르면 평가에 영향 | Evaluator가 별도 프로세스에서 엄격 평가 |
| 컨텍스트 오염: 구현 세부사항이 기획 판단을 흐림 | 에이전트 간 통신은 오직 파일로만 수행 |

### 구현 방법

- **구현 파일:**
  - `src/forge/agents/planner.py` -- Planner subprocess 래퍼
  - `src/forge/agents/evaluator.py` -- Evaluator subprocess 래퍼
  - `scaffold/CLAUDE.md` -- Generator 규칙
  - `scaffold/agents/planner/AGENT.md` -- Planner 규칙
  - `scaffold/agents/evaluator/AGENT.md` -- Evaluator 규칙
- **참고 문서:** [Claude Code Agents](https://docs.anthropic.com/en/docs/claude-code)

### 에이전트 입출력 데이터 플로우
![THE_FORGE 3-agent cycle](image/the_forge_architecture.svg)



### 3.1 Planner (기획자)

**실행 방식:** `claude -p --agent planner --max-turns {N} "{prompt}"`

**3가지 동작 모드:**

| 모드 | 트리거 조건 | 입력 | 출력 | max-turns |
|------|-----------|------|------|-----------|
| **A: 생성** | spec.md 없음 + 사용자 요청 | 사용자 요청, templates/*.md | spec.md, specs/*.md | planner_max_turns (15) |
| **B: 리뷰** | spec.md 존재 | spec.md, specs/*.md | plan-review.md (READY / NEEDS_REVISION) | planner_review_max_turns (10) |
| **C: Contract** | 오케스트레이터가 스프린트 시작 시 호출 | spec.md, specs/*, progress-log.md, sprint-*-done.md | sprint-contract.md (YAML frontmatter 포함) | contract_max_turns (12) |

**sprint-contract.md YAML frontmatter (v2.3 필수):**

```yaml
---
sprint_number: 3
has_next_sprint: true           # 이 스프린트 이후 더 할 게 있는가
estimated_remaining_sprints: 2  # 남은 예상 스프린트 수
next_sprint_preview: |          # 다음 스프린트 작업 예고
  news_analyst, tax_analyst 구현
  추가 4개 노드 병렬 통합
---
```

**절대 금지 사항:** 코드 작성 금지, spec.md 직접 수정 금지, 세부 구현(라이브러리 버전, 함수명) 지정 금지

### 3.2 Generator (구현자)

**실행 방식:** `claude` (interactive -- stdin/stdout 직접 연결, 사용자가 /exit으로 종료)

**세션 시작 절차 (CLAUDE.md에 의해 강제):**
1. `progress-log.md` 읽기 (가장 먼저 -- 이전 상태 파악)
2. `spec.md` 읽기 (전체 프로젝트 이해)
3. `specs/*.md` 중 현재 작업 관련만 읽기
4. `sprint-contract.md` 읽기 (이번 스프린트 범위)
5. `qa-report.md`가 있으면 읽고 FAIL 항목 먼저 수정
6. `git log --oneline -10` (최근 커밋 확인)

**작업 규칙:**
- 한 번에 하나의 기능만 구현
- sprint-contract.md에서 우선순위 높은 미완료 항목 선택
- 완료 시 해당 항목을 `[x]`로 체크
- 기능 하나 완성할 때마다 커밋 (`feat: [기능명]`)

**세션 종료 절차 (필수):**
1. 모든 변경사항 커밋
2. `progress-log.md` 맨 위에 세션 기록 추가
3. 앱을 작동하는 상태로 남기기

**파일 소유권 규칙:**

| 파일 | Generator 권한 |
|------|---------------|
| spec.md, plan-review.md, specs/* | 읽기 전용 |
| sprint-contract.md | 체크박스만 `[x]` 수정 |
| progress-log.md | 읽기/쓰기 (필수) |
| qa-report.md, harness-cost-log.txt | 읽기 전용 |
| decisions/*.md | 읽기/쓰기 |
| src/, tests/ | 전체 권한 |

**컨텍스트 소진 대응:** 컨텍스트가 길어지면 즉시 progress-log.md 업데이트 → 커밋 → `/exit`으로 세션 종료. progress-log.md + git history가 다음 세션의 인수인계 역할.

### 3.3 Evaluator (평가자)

**실행 방식:** `claude -p --agent evaluator --max-turns {N} "{prompt}"`

**평가 절차:**

| Step | 행동 | 상세 |
|------|------|------|
| 1 | 컨텍스트 수집 | spec.md, sprint-contract.md, specs/*, progress-log.md, 소스코드(Glob→Read) |
| 2 | 자동화 검증 | pytest, npm test, lint, 타입 체크, 서버 실행 시도 |
| 3 | 코드 리뷰 | sprint-contract.md 각 항목별 구현 여부, 명세 일치, 에러 핸들링, 엣지 케이스 |
| 4 | 채점 | 4차원 점수 (각 1-10점) |
| 5 | 보고서 | qa-report.md 생성 |

**4차원 채점 기준:**

| 기준 | FAIL 임계값 | 평가 대상 |
|------|------------|----------|
| 기능 완성도 | 7점 미만 | sprint-contract 항목별 구현 여부 |
| 코드 품질 | 7점 미만 | 네이밍, 구조, 에러 핸들링 |
| 테스트 커버리지 | 7점 미만 | 테스트 존재 여부 + 실행 결과 |
| 명세 충실도 | 7점 미만 | specs/*.md와의 일치도 |

**자기 합리화 방지 규칙:**
- "이 정도면 괜찮다" = 버그를 통과시키는 순간
- 파일명:라인 번호 없는 지적은 지적이 아니다
- 테스트를 실행할 수 있는데 안 한 채 PASS 금지
- 의심스러우면 FAIL이다

**Playwright E2E 자동 통합:** 프로젝트에 `playwright.config.{ts,js,mjs}`가 존재하면 자동으로 `npx playwright test`를 실행하고 결과를 qa-report.md에 첨부

---

## 4. artifacts/ 파일 기반 에이전트 간 통신

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 프로세스가 분리되면 메모리 공유 불가 | 파일이 유일한 통신 수단 -- 구조화된 프로토콜 필요 |
| 누가 어떤 파일을 수정하는지 규칙 없으면 충돌 발생 | 엄격한 소유권 매트릭스로 충돌 방지 |
| 이전 세션 상태를 알 방법 없음 | progress-log.md가 "미래의 자신에게 보내는 편지" 역할 |

### 구현 방법

- **구현 파일:** `src/forge/config.py` -- `ProjectPaths` 클래스
- **핵심 로직:** `ProjectPaths`가 `artifacts/`, `specs/`, `decisions/` 등의 경로를 일원 관리. 스프린트 번호는 `sprint-*-done.md`에서 최대 번호를 추출하여 계산.

### 문서별 소유자 역관계표

| 문서 | 생성자 | 읽는 주체 |
|------|--------|----------|
| `artifacts/spec.md` | Planner(Mode A) | Planner(B,C), Generator, Evaluator |
| `artifacts/plan-review.md` | Planner(Mode B) | 사용자(Telegram), forge(READY 확인) |
| `artifacts/specs/*.md` | Planner(Mode A) | Planner(B,C), Generator, Evaluator |
| `artifacts/sprint-contract.md` | Planner(Mode C) | Generator, Evaluator, forge |
| `artifacts/progress-log.md` | Generator | Generator(다음세션), Planner(C), Evaluator |
| `artifacts/qa-report.md` | Evaluator | Generator(다음세션), forge, 사용자 |
| `artifacts/decisions/*.md` | Generator | Evaluator, 사용자 |
| `artifacts/sprint-N-done.md` | forge | Planner(C) (이전 스프린트 참고) |
| `artifacts/harness-cost-log.txt` | forge(SprintTracer) | forge(예산 체크), 사용자 |
| `artifacts/.harness-checkpoint` | forge | forge(복구) |

**`progress-log.md`의 특별한 역할:**
- Generator가 자기 자신에게 보내는 편지 (다음 세션의 인수인계)
- 동시에 Planner와 Evaluator가 상황을 파악하는 유일한 채널
- forge가 세션 후 타임스탬프를 확인 -- Generator가 업데이트 안 했으면 경고

### Signal 파일 프로토콜

Telegram Receiver(데몬 스레드)와 Orchestrator(메인 스레드) 사이의 통신에 사용되는 파일:

| Signal 파일 | 생성 트리거 | Orchestrator 동작 |
|-------------|-----------|-------------------|
| `.approval-signal` | `/resume`, `/approve`, `/계속`, `/진행` | 다음 단계로 진행 |
| `.skip-signal` | `/skip`, `/스킵`, `/무시` | 리뷰 결과 무시하고 진행 |
| `.eval-signal` | `/eval`, `/재평가` | Evaluator만 재실행 |
| `.stop-signal` | `/stop`, `/중단` | 자동 루프 종료 |
| `.exit-signal` | `/exit`, `/종료` | 현재 세션 종료 |
| `.continue-signal` | `/continue` | 계속 진행 |

**동작 원리:** Orchestrator의 `wait_for_approval()` 함수가 루프 안에서 signal 파일의 존재 여부를 폴링. 파일이 감지되면 내용을 읽고 삭제한 후 해당 동작을 수행.

### artifacts/ 디렉토리 전체 구조

```
artifacts/
├── spec.md                    # 프로젝트 전체 설계서 (Planner 소유)
├── plan-review.md             # 설계 리뷰 결과 (Planner 소유)
├── sprint-contract.md         # 현재 스프린트 범위 (Planner 소유)
├── progress-log.md            # 세션 기록 (Generator 소유, 가장 중요)
├── qa-report.md               # QA 판정서 (Evaluator 소유)
├── harness-cost-log.txt       # 시간 + 토큰 로그 (forge 소유)
├── .harness-checkpoint        # 크래시 복구용 체크포인트 (forge 소유)
├── specs/                     # 도메인별 상세 스펙 (Planner 소유)
│   ├── langgraph-state.md
│   ├── db-schema.md
│   └── ...
├── decisions/                 # 의사결정 기록 (Generator 소유)
│   └── decision-001.md
├── sprint-1-done.md           # 완료된 스프린트 아카이브 (forge 소유)
├── sprint-2-done.md
├── .backup/                   # Telegram 파일 수신 시 백업
├── .approval-signal           # Telegram → forge 시그널
├── .skip-signal
├── .eval-signal
├── .stop-signal
├── .exit-signal
└── .continue-signal
```

---

## 5. 체크포인트 복구 시스템 (Checkpoint Recovery)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 장시간 자동 루프 중 터미널 크래시, OOM, 네트워크 끊김 발생 | 진행 상태 소실 방지 |
| 처음부터 다시 실행하면 이미 완료된 Phase 재실행으로 시간/토큰 낭비 | 정확한 중단 지점부터 재개 |
| bash의 `should_run_phase()` + `phase_order()` case 문이 복잡 | IntEnum 비교 한 줄로 단순화 |

### 구현 방법

- **구현 파일:** `src/forge/checkpoint.py` (전체 61줄)
- **핵심 로직:**
  - `Phase` IntEnum으로 9단계 정의
  - `should_run(target) = self.phase <= target` -- 한 줄 비교로 실행 여부 판단
  - 각 Phase 시작 직전에 `save()` → 크래시 시 해당 Phase부터 재개
  - FAIL 시 `phase`를 `CONTRACT_DONE`으로 자동 되돌림

### Phase 전이 다이어그램

```
NONE(0) ──> PLANNING(1) ──> PLANNING_DONE(2) ──> CONTRACT(3) ──> CONTRACT_DONE(4)
                                                                       │
                                          ┌────────────────────────────┘
                                          v
                                   GENERATING(5) ──> GENERATING_DONE(6)
                                                           │
                                          ┌────────────────┘
                                          v
                                   EVALUATING(7) ──> EVALUATING_DONE(8)
                                                           │
                                          ┌────────────────┘
                                          v
                                   PASS? ──┬── Yes + has_next ──> CONTRACT(3) (다음 스프린트)
                                           ├── Yes + 마지막 ──> 프로젝트 완료
                                           └── No (FAIL) ──> CONTRACT_DONE(4) (자동 롤백)
```

### 복구 시나리오별 동작

| 중단 시점 | 저장된 Phase | `forge run` 시 동작 |
|----------|-------------|-------------------|
| Planner 실행 중 | PLANNING(1) | Planner 재실행 |
| 사용자 검토 대기 중 | PLANNING_DONE(2) | Sprint Contract 생성부터 |
| Contract 생성 중 | CONTRACT(3) | Contract 재생성 |
| Generator 세션 중 | GENERATING(5) | Generator 재시작 (progress-log + git으로 복구) |
| Evaluator 실행 중 | EVALUATING(7) | Evaluator 재실행 |
| 결과 판정 대기 중 | EVALUATING_DONE(8) | 결과 판정만 수행 |

**`forge run --from PHASE` 명시적 오버라이드:**

```bash
forge run --from generating    # Generator부터 강제 시작
forge run --from evaluating    # Evaluator부터 강제 시작
forge run --from planning      # 처음부터 재시작
```

이 옵션은 체크포인트를 해당 Phase 직전으로 자동 되돌린 뒤 실행한다.

### .harness-checkpoint 파일 형식

```json
{
  "phase": 5,
  "phase_name": "GENERATING",
  "detail": "generator session running",
  "timestamp": "2026-04-16T14:40:00+09:00"
}
```

---

## 6. Telegram 양방향 통신 (Bidirectional Telegram)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 야간 자동 루프 중 승인/중단을 터미널에서만 가능 | 모바일에서 원격 제어 필수 |
| bash의 curl + jq로 Telegram API 호출은 jq 의존성 문제 | httpx로 의존성 최소화 |
| 단방향 알림만으로는 FAIL 대응 불가 | 양방향 통신: 알림 + 명령 수신 + 파일 업로드 |

### 구현 방법

- **구현 파일:** `src/forge/telegram/notifier.py`, `src/forge/telegram/receiver.py`
- **참고 문서:** [Telegram Bot API](https://core.telegram.org/bots/api), [httpx](https://www.python-httpx.org/)

### 6.1 Notifier (알림 송신)

모든 알림을 Telegram으로 전송. 파일 첨부를 기본으로 하며, 이벤트별 이모지가 자동 매핑된다.

**알림 시점 전체 목록:**

| 시점 | 이벤트 타입 | 첨부 파일 | 사용자 행동 |
|------|-----------|----------|-----------|
| Planner 생성 완료 | `planner_done` | spec.md | 검토 후 /resume |
| 도메인 스펙 생성 | `spec_detail` | specs/*.md | 검토 |
| 리뷰 NEEDS_REVISION | `plan_revision` | plan-review.md | 수정 후 /resume |
| 첫 Sprint Contract | `sprint_contract` | sprint-contract.md | 검토 후 /resume |
| Generator 세션 시작 | `generator_start` | sprint-contract.md | -- |
| Generator 시간 초과 | `warning` | -- | /exit |
| QA PASS + 다음 있음 | `sprint_pass_next` | qa-report.md | /resume 또는 /stop |
| QA PASS + 프로젝트 완료 | `project_complete` | qa-report.md | -- (자동 종료) |
| QA FAIL | `qa_fail` | qa-report.md | /resume, /eval, /stop |
| 연속 FAIL 자동 중단 | `auto_stop` | qa-report.md | 수동 개입 |
| 누적 예산 초과 | `budget_exceeded` | cost-log.txt | 수동 개입 |
| 예기치 않은 중단 | `error` | checkpoint | forge run |

### 6.2 Receiver (명령 수신)

`TelegramReceiver` 클래스가 데몬 스레드로 백그라운드에서 long polling 실행.

**동작 흐름:**

```
TelegramReceiver.start()
    │
    v
데몬 스레드에서 _poll_loop() 실행
    │
    ├── getUpdates (25초 long polling)
    │     │
    │     v
    │   _handle_update(update)
    │     │
    │     ├── chat_id 검증 (설정된 ID만 허용)
    │     │
    │     ├── 텍스트 메시지 ──> _handle_command()
    │     │     │
    │     │     ├── /resume ──> .approval-signal 생성
    │     │     ├── /eval ──> .eval-signal 생성
    │     │     ├── /stop ──> .stop-signal 생성
    │     │     ├── /skip ──> .skip-signal + .approval-signal
    │     │     ├── /status ──> 현재 상태 인라인 전송
    │     │     └── /help ──> 명령어 목록 전송
    │     │
    │     └── 파일 메시지 ──> _handle_file()
    │           │
    │           ├── caption 파싱 (경로 결정)
    │           ├── path traversal 검증
    │           ├── 기존 파일 .backup/에 백업
    │           ├── Telegram API로 파일 다운로드
    │           └── 확인 메시지 전송
    │
    └── 반복
```

### 6.3 Telegram 파일 업로드

외출 중 폰에서 파일을 수정하여 봇에게 전송할 수 있다.

**caption 규칙:**

| caption | 저장 위치 | 용도 |
|---------|----------|------|
| 없음 | `artifacts/{파일명}` | 빠르게 아무 파일이나 넣을 때 |
| `artifacts/spec.md` | `artifacts/spec.md` | 스펙 수정본 업로드 |
| `specs/db.md` | `artifacts/specs/db.md` | 단축 표기 |

기존 파일이 있으면 자동으로 `artifacts/.backup/{파일명}.{timestamp}.bak`에 백업 후 덮어쓰기.

### 6.4 Telegram 알림 메시지 예시

**QA PASS 후 알림:**
```
✅ Sprint 3 완료

점수:
  기능 완성도: 8/10
  코드 품질: 7/10
  테스트 커버리지: 6/10
  명세 충실도: 8/10

📊 비용
  이번 스프린트: 142분 | in 1,264,681 / out 198,379
  누적: 387분

📋 Sprint 4 계획
  news_analyst, tax_analyst 구현
  추가 4개 노드 병렬 통합

/resume — 계속 진행
/stop — 여기서 중단
```

**QA FAIL 후 알림:**
```
❌ Sprint 3 FAILED

점수:
  기능 완성도: 5/10
  코드 품질: 6/10
  테스트 커버리지: 3/10
  명세 충실도: 5/10

주요 실패 항목:
  analyze_node만 구현, 나머지 2개 미구현
  엣지 조건 분기 누락

⚠️ 연속 FAIL: 1/3 (3회 시 자동 중단)

/resume — Generator 재진입하여 수정
/eval — Evaluator 기준 조정 후 재평가만
/stop — 여기서 중단
```

---

## 7. 비용 투명성과 관측성 (Cost Transparency & Observability)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 자동 루프가 토큰을 얼마나 소모하는지 파악 불가 | 무한 루프 시 리소스 낭비 방지 |
| 스프린트별 ROI 판단 근거 없음 | 에이전트별 시간 + 토큰 추적 필요 |
| Max 플랜은 토큰 과금이 없지만, 시간은 유한 | 시간 기반 예산 관리 |

### 구현 방법

- **구현 파일:** `src/forge/cost_tracker.py`
- **핵심 로직:** `SprintTracer` 클래스가 context manager 패턴으로 각 에이전트 실행을 감싸서 시간과 토큰을 추적
- **참고 문서:** [Langfuse Python SDK](https://langfuse.com/docs/sdk/python)

### 7.1 이중 토큰 추출 메커니즘

두 가지 실행 방식에 따라 토큰 추출 경로가 다르다:

| 에이전트 | 실행 방식 | 토큰 추출 방법 |
|---------|----------|--------------|
| Planner, Evaluator | `claude -p` (subprocess) | stdout에서 정규식 파싱: `Tokens: input X / output Y / cache Z` |
| Generator | `claude` (interactive) | `~/.claude/projects/{project-id}/*.jsonl`에서 시간 범위 내 usage 필드 합산 |

**subprocess 토큰 추출 (stdout 파싱):**
```
claude -p --agent planner "..."  실행
  → stdout 마지막에: "Tokens: input 45,231 / output 12,456 / cache 2,100"
  → 정규식으로 파싱하여 숫자 추출
```

**interactive 토큰 추출 (JSONL 파싱):**
```
~/.claude/projects/{project-id}/*.jsonl 파일에 세션별 기록
  → 각 줄의 assistant 메시지에 usage 필드 포함:
    {"type":"assistant","usage":{"input_tokens":...,"output_tokens":...}}
  → 세션 시작~종료 시간 범위로 필터링하여 합산
```

### 7.2 harness-cost-log.txt

Langfuse 키 설정 여부와 무관하게 항상 텍스트 로그로 기록:

```
[2026-04-15T14:30:00] sprint-3 planner   | 272s    | in    45,231 / out   12,456 / cache   2,100 | generate   | ok
[2026-04-15T14:38:00] sprint-3 contract  | 135s    | in    28,104 / out    5,678 / cache       0 | planner-c  | ok
[2026-04-15T14:40:00] sprint-3 generator | 7623s   | in 1,123,456 / out  156,789 / cache  89,000 | interactive| ok
[2026-04-15T16:50:00] sprint-3 evaluator | 524s    | in    67,890 / out   23,456 / cache   5,200 | claude-p   | ok
─────────────────────────────────────────────────────────────────────────────────
Sprint 3 합계: 142m34s | in 1,264,681 / out 198,379
```

### 7.3 Langfuse 듀얼레이어 트레이싱

THE FORGE는 두 개의 독립적인 Langfuse 추적 레이어를 운영한다:

```
┌─ Layer 1: 하네스 메타 추적 (forge가 관리) ──────────────┐
│                                                         │
│  .env의 FORGE_LANGFUSE_* 키로 설정                      │
│                                                         │
│  Sprint Trace (1 스프린트 = 1 Trace)                    │
│    ├── Planner Span    (4분, 45K tokens)                │
│    ├── Contract Span   (2분, 28K tokens)                │
│    ├── Generator Span  (127분, 1.1M tokens)             │
│    └── Evaluator Span  (8분, 67K tokens)                │
│                                                         │
│  → "이 프로젝트의 3번째 스프린트에 얼마나 걸렸나"        │
└─────────────────────────────────────────────────────────┘
        ↕ 완전 별개 — 서로 독립적으로 동작
┌─ Layer 2: 프로젝트 코드 내 추적 (Generator가 구현) ─────┐
│                                                         │
│  대상 프로젝트 자체의 Langfuse 키로 설정                 │
│  specs/langfuse-tracing.md에 따라 Generator가 코드에 삽입│
│                                                         │
│  → "프로젝트 코드가 LLM을 어떻게 사용하고 있나"         │
└─────────────────────────────────────────────────────────┘
```

**Layer 1은 선택적 의존성:** Langfuse 키가 설정되지 않아도 harness-cost-log.txt에는 항상 기록된다.

---

## 8. 프로젝트 부트스트래핑 (forge init Scaffold 시스템)

### 선택 이유

| 기존 방식의 한계 | 해결 필요성 |
|-----------------|------------|
| 매 프로젝트마다 CLAUDE.md, AGENT.md, templates 수동 작성 | 반복 작업 제거 |
| 프로젝트마다 harness/ 폴더 복사 (v1.x) | 도구로 1회 설치, scaffold는 초기화만 |
| Hooks 설정을 수동으로 해야 함 | settings.json 자동 머지 |

### 구현 방법

- **구현 파일:** `src/forge/cli.py` -- `init` 함수 및 헬퍼들
- **핵심 로직:**
  1. `_scaffold_dir()`: THE_FORGE 패키지 내 scaffold/ 디렉토리 탐색
  2. `_copy_scaffold()`: CLAUDE.md, AGENT.md, templates/ 복사
  3. `_merge_claude_settings()`: `.claude/settings.json` hooks 스마트 머지 (중복 방지)
  4. `_ensure_env_and_pyproject()`: `.env` 템플릿 + `pyproject.toml [tool.forge]` 섹션 추가
  5. `_ensure_gitignore()`: artifacts/ 경로들을 `.gitignore`에 추가

### forge init 실행 흐름

```
forge init
    │
    ├── scaffold/ 디렉토리 탐색 (개발 모드 vs 설치 모드)
    │
    ├── CLAUDE.md 복사 → 프로젝트 루트
    │
    ├── .claude/agents/planner/AGENT.md 복사
    ├── .claude/agents/evaluator/AGENT.md 복사
    │
    ├── templates/ 전체 복사
    │   ├── INDEX.md
    │   ├── sprint-contract-template.md
    │   ├── generator-guide.md
    │   ├── langgraph-agent.md
    │   ├── db-setup.md
    │   ├── deepeval-setup.md
    │   └── langfuse-setup.md
    │
    ├── .claude/settings.json 머지 (기존 hooks 보존 + forge hooks 추가)
    │   ├── PostToolUse: Edit|Write → ruff check --fix (자동 린트)
    │   └── Stop: * → forge notify session_stop (세션 종료 알림)
    │
    ├── .env 생성 (비밀 필드 빈 값으로 템플릿)
    │
    ├── pyproject.toml [tool.forge] 섹션 추가 (기본값)
    │
    └── .gitignore에 artifacts/ 경로 추가
```

### forge init --force (업데이트 시)

THE_FORGE를 업데이트한 후 scaffold 변경사항을 기존 프로젝트에 전파할 때 사용:
- 기존 파일을 `artifacts/.backup/`에 타임스탬프 포함하여 백업
- scaffold 원본으로 덮어쓰기
- 사용자가 커스터마이징한 내용은 `.backup/`에서 복구 가능

### templates/ 선택적 참조 시스템

Planner가 템플릿을 읽는 효율적 3단계 프로세스:

| 단계 | 읽는 파일 | 소모 턴 | 목적 |
|------|----------|---------|------|
| 1 | `templates/INDEX.md` | 1턴 | 전체 템플릿 목차에서 후보 선정 |
| 2 | 후보 파일의 frontmatter | 0.5턴 | 키워드 매칭으로 정밀 필터링 |
| 3 | 최종 선택된 템플릿 본문 | 1턴 x N개 | 실제 내용 참고 |

불필요한 템플릿을 모두 읽는 것을 방지하여 토큰 효율성 확보.

### 설정 로드 우선순위

```
1. 환경변수 (최우선)        예: FORGE_TELEGRAM_BOT_TOKEN=...
2. .env 파일               예: FORGE_TELEGRAM_BOT_TOKEN="..."
3. forge.toml [forge]      (v2.2 이전 레거시, 하위 호환)
4. pyproject.toml [tool.forge]  예: max_sprint_minutes = 180
5. 코드 기본값 (최하위)
```

`.env` (비밀 전용, git 미커밋) + `pyproject.toml` (공유 설정, git 커밋) 조합으로 비밀과 설정을 분리.

---

## 9. 실전 사용 가이드 (다른 프로젝트에서 도구로 사용하기)

### 9.1 새 프로젝트 시작 흐름

```bash
# 1. 프로젝트 디렉토리 생성 및 이동
mkdir my-agent-project && cd my-agent-project
git init

# 2. THE FORGE로 프로젝트 초기화
forge init

# 3. .env에 Telegram/Langfuse 토큰 입력 (선택)
#    Telegram 없이도 동작 가능 (터미널에서 Enter 키로 승인)

# 4. 하네스 실행 (프로젝트 완료까지 자동 루프)
forge run "7개 병렬 분석 에이전트로 부동산 데이터를 수집하는 LangGraph 시스템"
```

### 9.2 CLI 명령어 전체 레퍼런스

| 명령어 | 설명 | 주요 옵션 |
|--------|------|----------|
| `forge run [REQUEST]` | 메인 사이클 실행 (자동 루프) | `--plan PATH`: 기획서 파일 지정 |
| | | `--from PHASE`: 특정 단계부터 시작 (`planning` / `contract` / `generating` / `evaluating`) |
| | | `--single-sprint`: 1 스프린트만 실행 |
| | | `--max-sprints N`: 최대 N개 스프린트 |
| | | `--root PATH`: 프로젝트 루트 지정 |
| `forge eval` | Evaluator만 단독 재실행 | `--root PATH` |
| `forge status` | 현재 하네스 상태 확인 | `--root PATH` |
| `forge init` | 프로젝트 부트스트랩 | `--force`: 기존 파일 백업 후 덮어쓰기 |
| `forge --help` | 전체 명령어 도움말 | |
| `forge --version` | 설치된 버전 확인 | |

**`forge run` 사용 패턴:**

| 패턴 | 상황 | 명령어 |
|------|------|--------|
| A | 짧은 요청 (자동 루프) | `forge run "LangGraph 에이전트 만들어줘"` |
| B | 기획서 파일 있음 | `forge run --plan ./my-plan.md` |
| C | 이전 세션 계속 | `forge run` (체크포인트 자동 감지) |
| D | 중단 후 복구 | `forge run` (체크포인트에서 재개) |
| E | 1 스프린트만 | `forge run --single-sprint` |
| F | 최대 N 스프린트 | `forge run --max-sprints 3` |
| G | 특정 단계부터 | `forge run --from generating` |

### 9.3 Telegram 명령어 전체 레퍼런스

| 명령어 | 한국어 별칭 | 동작 | 사용 가능 시점 |
|--------|-----------|------|-------------|
| `/resume` | `/계속`, `/진행` | 대기 중인 승인 통과 | PASS 후, FAIL 후, 리뷰 후 |
| `/eval` | `/재평가` | Evaluator만 재실행 | FAIL 상태에서만 |
| `/stop` | `/중단` | 자동 루프 종료 | 승인 대기 중 언제든 |
| `/skip` | `/스킵`, `/무시` | 리뷰 결과 무시하고 진행 | NEEDS_REVISION 한정 |
| `/status` | `/상태` | 현재 상태 + 누적 비용 전송 | 언제든 |
| `/help` | `/도움` | Telegram 명령 도움말 | 언제든 |

**파일 전송 시 caption 규칙:**
- caption 없이 전송 → `artifacts/{파일명}`에 저장
- `artifacts/spec.md` → 해당 경로에 덮어쓰기 (기존 파일 자동 백업)
- `specs/db.md` → `artifacts/specs/db.md` (단축 표기)

### 9.4 체크포인트 상태별 forge run 동작

| 현재 체크포인트 | `forge run` 실행 결과 |
|---------------|---------------------|
| NONE / 없음 | Phase 1(Planning)부터 전체 실행 |
| PLANNING_DONE | Sprint Contract 생성부터 |
| CONTRACT_DONE | Generator부터 (FAIL 시 여기로 자동 롤백됨) |
| GENERATING_DONE | Evaluator부터 |
| EVALUATING_DONE | 결과 판정만 수행 |

### 9.5 상황별 대처법

**QA FAIL 시:**

```
상황: Sprint N이 FAIL 판정을 받음
  │
  ├── Telegram 알림 수신 (qa-report.md 첨부)
  │     점수 + 실패 항목 + 선택지 표시
  │
  ├── 선택지 1: /resume
  │     → Generator가 재진입하여 FAIL 항목 수정
  │     → qa-report.md의 FAIL 항목을 먼저 읽고 수정 시작
  │
  ├── 선택지 2: /eval
  │     → AGENT.md(Evaluator 규칙)를 수정한 후 사용
  │     → 평가 기준 조정 후 동일 코드를 재평가만
  │     → 결과에 따라 다시 PASS/FAIL 판정
  │
  └── 선택지 3: /stop
        → 자동 루프 종료
        → 나중에 forge run으로 재개 가능 (체크포인트 보존)
```

**예기치 않은 크래시 시:**

```
상황: 터미널 종료, OOM, 네트워크 끊김 등
  │
  └── forge run  (그냥 다시 실행하면 됨)
        → .harness-checkpoint에서 중단 Phase 읽기
        → 해당 Phase부터 자동 재개
        → 이미 완료된 Phase는 건너뜀
```

**누적 예산 초과 시:**

```
상황: 누적 시간이 max_total_minutes(기본 1440분 = 24시간) 초과
  │
  ├── 자동 중단 + Telegram 알림 (cost-log.txt 첨부)
  │
  └── 대처 방법:
      ├── pyproject.toml의 max_total_minutes 값 증가
      └── forge run으로 재시작 (체크포인트에서 재개)
```

**NEEDS_REVISION (설계 수정 필요) 시:**

```
상황: Planner 리뷰가 NEEDS_REVISION 판정
  │
  ├── Telegram 알림 (plan-review.md 첨부)
  │
  ├── 선택지 1: spec.md 수정 후 /resume
  │     → Telegram으로 수정된 spec.md 파일 전송 (caption: artifacts/spec.md)
  │     → /resume 입력 → Planner가 재리뷰
  │
  └── 선택지 2: /skip
        → 리뷰 결과 무시하고 Sprint Contract 생성 진행
```

**연속 FAIL 3회 자동 중단 시:**

```
상황: 같은 스프린트에서 3회 연속 FAIL
  │
  ├── 자동 중단 + Telegram 알림
  │
  └── 대처 방법:
      ├── qa-report.md 확인 → 근본 원인 파악
      ├── AGENT.md(Evaluator) 수정 → 평가 기준 조정
      ├── 또는 직접 코드 수정 후 forge run --from evaluating
      └── 또는 pyproject.toml의 max_consecutive_fails 값 증가
```

### 9.6 설정 필드 전체 목록

**비밀 설정 (.env -- git 커밋 금지):**

| 필드 | 환경변수 | 기본값 | 설명 |
|------|---------|--------|------|
| `telegram_bot_token` | `FORGE_TELEGRAM_BOT_TOKEN` | `""` | Telegram Bot API 토큰 |
| `telegram_chat_id` | `FORGE_TELEGRAM_CHAT_ID` | `""` | 알림 받을 채팅 ID |
| `langfuse_public_key` | `FORGE_LANGFUSE_PUBLIC_KEY` | `""` | Langfuse 공개 키 |
| `langfuse_secret_key` | `FORGE_LANGFUSE_SECRET_KEY` | `""` | Langfuse 비밀 키 |
| `langfuse_host` | `FORGE_LANGFUSE_HOST` | `"https://cloud.langfuse.com"` | Langfuse 서버 주소 |

**공유 설정 (pyproject.toml [tool.forge] -- git 커밋 가능):**

| 필드 | 기본값 | 설명 |
|------|--------|------|
| `max_sprint_minutes` | 180 | 스프린트당 최대 시간 (분) |
| `max_generator_minutes` | 120 | Generator 세션 최대 시간 (분) |
| `planner_max_turns` | 15 | Planner(생성) Claude Code 턴 제한 |
| `planner_review_max_turns` | 10 | Planner(리뷰) 턴 제한 |
| `contract_max_turns` | 12 | Contract 생성 턴 제한 |
| `evaluator_max_turns` | 20 | Evaluator 턴 제한 |
| `playwright_enabled` | true | Playwright E2E 테스트 자동 실행 |
| `playwright_timeout_seconds` | 600 | Playwright 타임아웃 |
| `max_total_minutes` | 1440 | 전체 프로젝트 누적 시간 상한 (24시간) |
| `max_consecutive_fails` | 3 | 연속 FAIL 허용 횟수 |
| `max_total_sprints` | 20 | 전체 프로젝트 스프린트 수 상한 |

---

## 부록: 용어 사전

| 용어 | 설명 |
|------|------|
| **Harness** | AI 에이전트를 오케스트레이션하는 제어 시스템. THE FORGE는 Claude Code CLI 위에 동작하는 "harness-over-harness" |
| **Sprint** | 하나의 작업 사이클. Contract 생성 → Generator 구현 → Evaluator 평가까지가 1 스프린트 |
| **Phase** | 오케스트레이터의 실행 단계. NONE(0)부터 EVALUATING_DONE(8)까지 9단계 |
| **Checkpoint** | `.harness-checkpoint` 파일에 저장되는 현재 Phase. 크래시 시 복구에 사용 |
| **artifacts/** | 에이전트 간 통신에 사용되는 모든 파일이 저장되는 디렉토리 |
| **Signal File** | Telegram Receiver와 Orchestrator 사이의 통신용 빈 파일 (`.approval-signal` 등) |
| **Sprint Contract** | 이번 스프린트에서 구현할 항목 목록. YAML frontmatter에 `has_next_sprint` 포함 |
| **progress-log.md** | Generator가 세션마다 작성하는 작업 기록. "미래의 자신에게 보내는 편지" |
| **scaffold/** | `forge init` 시 프로젝트에 복사되는 템플릿 원본 디렉토리 |
| **CLAUDE.md** | Generator(메인 세션)의 행동 규칙 파일. Claude Code가 자동 로드 |
| **AGENT.md** | 서브에이전트(Planner, Evaluator)의 행동 규칙 파일 |
| **SprintTracer** | 스프린트 단위로 시간과 토큰을 추적하는 context manager |
| **Max 플랜** | Anthropic의 Claude 정액제 구독. API 과금 없이 CLI 사용 가능 |

---

## 요약 테이블

| 기술/기능 | 선택 이유 | 구현 방법 | 핵심 특성 |
|----------|----------|----------|----------|
| **전체 아키텍처** | API 과금 회피 + 역할 격리 | subprocess로 Claude Code CLI 호출 | Max 플랜 쿼터 내 동작, API 비용 0원 |
| **자동 스프린트 루프** | 수동 재실행 제거 + 야간 자동화 | `run_cycle()` while True + Telegram 승인 | 안전장치 4종, has_next_sprint 자동 판단 |
| **3-에이전트 파이프라인** | 자기 합리화 방지 | Planner/Evaluator(subprocess) + Generator(interactive) | 프로세스 격리, 4차원 채점, 자기합리화 방지 규칙 |
| **artifacts/ 파일 통신** | 프로세스 분리 시 메모리 공유 불가 | 문서 소유권 매트릭스 + signal 파일 | progress-log.md 중심 인수인계, signal 파일 6종 |
| **체크포인트 복구** | 크래시 시 진행 상태 소실 방지 | Phase IntEnum + `should_run()` 단조 비교 | 자동 복구, FAIL 시 롤백, `--from` 오버라이드 |
| **Telegram 양방향 통신** | 외출 중 원격 제어 | httpx Notifier + daemon thread Receiver | 명령어 7종 + 한국어 별칭, 파일 업로드, path traversal 방어 |
| **비용 투명성** | 무한 루프 리소스 낭비 방지 | stdout 파싱 + JSONL 파싱 이중 추출 | harness-cost-log.txt + Langfuse 듀얼레이어 |
| **scaffold 부트스트래핑** | 반복적 초기 설정 제거 | `forge init`으로 1회 복사 + settings.json 머지 | templates INDEX.md 선택적 참조, .env/pyproject.toml 분리 |
